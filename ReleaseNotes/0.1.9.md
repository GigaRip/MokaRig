### New
- Attach Mouse: an option to add a USB mouse to a macOS VM alongside the trackpad, giving the guest a plain pointer and its own Mouse pane in System Settings.

### Improved
- macOS guests now use a Mac keyboard and Mac trackpad instead of generic USB devices — proper Apple key mapping (Globe/Fn, media keys) and real trackpad gestures for closer fidelity with the host.
